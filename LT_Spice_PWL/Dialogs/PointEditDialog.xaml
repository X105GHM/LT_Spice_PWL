<Window x:Class="PwlEditor.Dialogs.PointEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Punkt bearbeiten"
        Height="250"
        Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}" ShowInTaskbar="False" WindowStyle="ToolWindow">

    <Grid Margin="16" Background="{DynamicResource WindowBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="90"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Margin="0,0,8,12"
                   VerticalAlignment="Center"
                   Text="Zeit (X):"/>

        <TextBox x:Name="XTextBox"
                 Grid.Row="0"
                 Grid.Column="1"
                 Margin="0,0,0,12"/>

        <TextBlock Grid.Row="1"
                   Grid.Column="0"
                   Margin="0,0,8,12"
                   VerticalAlignment="Center"
                   Text="Wert (Y):"/>

        <TextBox x:Name="YTextBox"
                 Grid.Row="1"
                 Grid.Column="1"
                 Margin="0,0,0,12"/>

        <TextBlock x:Name="RuleTextBlock"
                   Grid.Row="2"
                   Grid.ColumnSpan="2"
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource InfoTextBrush}"/>

        <StackPanel Grid.Row="3"
                    Grid.ColumnSpan="2"
                    Margin="0,16,0,0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Width="90"
                    Margin="0,0,8,0"
                    Click="OkButton_Click">OK</Button>
            <Button Width="90"
                    IsCancel="True">Abbrechen</Button>
        </StackPanel>
    </Grid>
</Window>